EAPI=5
DESCRIPTION="Base configuration for Maru (probably intended for OEM, but we're abusing it anyways)."

LICENSE="BSD-Google"
SLOT="0"
KEYWORDS="*"
IUSE=""
S="${WORKDIR}"

RDEPEND=""
DEPEND="${RDEPEND}"